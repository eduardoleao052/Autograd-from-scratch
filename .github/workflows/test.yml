on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: TestFramework
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2    # checkout the repo
      - run: python /src/test_framework.py
