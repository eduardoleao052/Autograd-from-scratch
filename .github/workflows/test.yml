on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: TestFramework
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2    # checkout the repo
      - run: cd src
      - run: pwd
      - run: ls
      - run: python test_framework.py
