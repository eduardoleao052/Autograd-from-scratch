on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Test framework
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2    # checkout the repo
      - name: Install dependencies
        run: pip install -r requirements.txt
      - run: python /home/runner/work/Autograd-from-scratch/Autograd-from-scratch/src/test_framework.py
